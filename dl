from keras.datasets import imdb

from keras.preprocessing.text import Tokenizer

from keras.utils import pad sequences

from keras import Sequential

from keras.layers import

Dense, SimpleRNN, Embedding, Flatten (X_train, y train), (X test

y_test) = imdb.load data() X train =

pad sequences (X train, padding='post', maxlen=50)

pad sequences (X test, padding='post', maxlen=50)

X train.shape

model Sequential ()

#model.add (Embedding (10000, 2))

model.add (SimpleRNN (32, input shape= (50, 1),

model.add (Dense (1, activation='sigmoid'))

model. summary ()

model.compile (optimizer='adam', loss='binary crossentropy', metrics=('acc')) model. fit (X train, y train, epochs=5, validation data=(X test,y test)) test loss, test acc = model.evaluate (X test, y test) print ('Test loss', test loss)

print ('Test accuracy', test acc)